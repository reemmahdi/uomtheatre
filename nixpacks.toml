[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "nodejs_20"]

[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "php artisan config:clear && php artisan migrate:fresh --force --seed && php -S 0.0.0.0:$PORT -t public public/index.php"
